<%= form_for(@analise) do |f| %>
  <% if @analise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@analise.errors.count, "error") %> prohibited this analise from being saved:</h2>

      <ul>
      <% @analise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
    <div class="field">
    <%= f.label :'Seleccione el nombre del paciente' %><br>
    <%= f.collection_select(:user_id, User.all(), :id, :nombre, {:required => "required"}, :class => "textboxgrade" ) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
